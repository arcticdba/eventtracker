services:
  eventtracker:
    build: .
    container_name: eventtracker
    ports:
      - "3333:3000"
    volumes:
      - eventtracker-data:/data
    restart: unless-stopped

volumes:
  eventtracker-data:
