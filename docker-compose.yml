services:
  eventtracker:
    build: .
    container_name: eventtracker
    ports:
      - "3000:3000"
    volumes:
      - eventtracker-data:/data
    restart: unless-stopped

volumes:
  eventtracker-data:
